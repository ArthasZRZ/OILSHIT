<Window x:Class="WpfRibbonApplication1.HeatDoubleImporter"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HeatDoubleImporter" Height="500" Width="650"
        >
    <Window.Resources>
        
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Name="BtnImporter" Content="导入" Click="BtnImporter_Click_1" Grid.Row="0"
                Width="50" HorizontalAlignment="Left" Margin="5,5,5,5"></Button>
        <DataGrid Grid.Row="1"  x:Name="ShowImportedData" AutoGenerateColumns="False" CanUserAddRows = "false">
            <DataGrid.Columns>
                <DataGridTextColumn Header="名称" Width="*" Binding="{Binding Path=Name}"></DataGridTextColumn>
                <DataGridTextColumn Header="X" Width="140" Binding="{Binding Path=Xstr}"></DataGridTextColumn>
                <DataGridTextColumn Header="Y" Width="140" Binding="{Binding Path=Ystr}"></DataGridTextColumn>
                <DataGridTextColumn Header="Z" Width="140" Binding="{Binding Path=Zstr}"></DataGridTextColumn>
            </DataGrid.Columns>
            
        </DataGrid>
    </Grid>
</Window>
